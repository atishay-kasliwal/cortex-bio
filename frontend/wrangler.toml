/*
  Cloudflare Pages — Atriveo Bio
  https://bio.atriveo.com (production)
  https://preview.bio.atriveo.com (preview)
*/
name = "atriveo-bio"
compatibility_date = "2024-01-01"
pages_build_output_dir = ".output/public"

# GitHub → Cloudflare Pages handles CI/CD.
# See LAUNCH_CLOUDFLARE.md for DNS setup (fixes NXDOMAIN).

[env.production]
vars = { ENVIRONMENT = "production" }

[env.preview]
vars = { ENVIRONMENT = "preview" }
