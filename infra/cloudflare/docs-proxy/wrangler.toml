name = "cortex-bio-docs"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Route: docs.cortex.bio/*
# Set in Cloudflare dashboard after deploy:
#   Workers & Pages → cortex-bio-docs → Settings → Triggers → Custom Domain

[vars]
API_ORIGIN = "https://api.atriveo.com"
